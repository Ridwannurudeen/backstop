[package]
name = "risk_feed"
edition = "2024.beta"

# Modern Sui CLIs (>= 1.45) resolve the Sui framework as an implicit system
# dependency, so no [dependencies] block is needed. If an older CLI complains,
# add:
#   [dependencies]
#   Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

[addresses]
risk_feed = "0x0"
